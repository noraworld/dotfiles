# frozen_string_literal: true

# IMPORTANT NOTE:
#   Do not add RubyGems to be intended to be used only in dotfiles,
#   because RubyGems that are added here and have CLI will be used globally,
#   which can cause unintended version use of tools,
#   such as RuboCop, Pry, etc., in other Ruby projects

source 'https://rubygems.org'

gem 'pokemon', '>= 0.1.0'
