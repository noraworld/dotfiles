current_directory=$(pwd)

if [[ $1 =~ ^[0-9]+$ ]]; then
  popd "+$@" 1>/dev/null || echo
  store_directory_stack
else
  popd  "$@" 1>/dev/null || echo
  store_directory_stack
fi

show_directory_stack
